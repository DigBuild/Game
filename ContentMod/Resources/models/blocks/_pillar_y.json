{
  "variables": {
    "textures": {
      "neg_x": "${textures/side}",
      "pos_x": "${textures/side}",
      "neg_y": "${textures/end}",
      "pos_y": "${textures/end}",
      "neg_z": "${textures/side}",
      "pos_z": "${textures/side}"
    }
  },
  "components": {
    "cube": {
      "_type": "digbuild:include",
      "from": "digbuild:blocks/_cube"
    }
  }
}